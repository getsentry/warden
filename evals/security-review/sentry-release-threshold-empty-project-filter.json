{
  "given": "release threshold endpoint builds an empty ORM filter when the caller has no accessible projects",
  "files": [
    "fixtures/sentry-release-threshold-empty-project-filter/release_threshold_index.py"
  ],
  "should_find": [
    {
      "finding": "ReleaseThreshold query omits project and organization scoping when get_projects returns an empty list, allowing cross-project or cross-organization threshold disclosure"
    }
  ],
  "should_not_find": [
    "generic missing serializer validation",
    "missing rate limiting"
  ]
}
