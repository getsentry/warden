---
title: Quickstart
description: Install Warden, configure model credentials, and initialize a repository.
editUrl: false
---

import CopyPrompt from "@sentry/starlight-theme/components/CopyPrompt.astro";

export const setupPrompt = `Set up Warden completely for this repository. Do not stop after installing the CLI or generating starter files.

Use the current Warden documentation as the source of truth. Read these pages before editing anything:

- Quickstart: https://warden.sentry.dev/quickstart/
- Repository setup and secrets: https://warden.sentry.dev/github/repository/
- Models and runtimes: https://warden.sentry.dev/config/models/
- Adding skills: https://warden.sentry.dev/skills/adding/
- Skill configuration and path filters: https://warden.sentry.dev/config/skills/
- Pull request triggers: https://warden.sentry.dev/config/triggers/
- Reporting and failure thresholds: https://warden.sentry.dev/config/output/
- GitHub Actions workflow: https://warden.sentry.dev/github/workflow/
- Local review and validation: https://warden.sentry.dev/local-review/

Then do the following:

1. Read this repository's agent instructions, package-manager files, languages, source layout, generated or vendored directories, test layout, and existing GitHub workflows. Check for an existing \`warden.toml\`, \`.warden/\`, Warden workflow, or Warden secrets documentation. Preserve and improve an existing setup instead of replacing it blindly.
2. Confirm Node.js 22.12 or newer is available. Use the repository's package manager to run the latest \`@sentry/warden\` CLI. Initialize Warden when needed with the documented \`init\` command.
3. Configure \`warden.toml\`, not just the workflow. Add the built-in \`security-review\` and \`code-review\` skills unless equivalent reviews already exist. Use \`warden add\` where appropriate. Tailor each skill's \`paths\` and \`ignorePaths\` to this repository so Warden reviews real source and configuration while excluding dependencies, generated artifacts, build output, fixtures, snapshots, and other low-value files. Keep any existing custom or remote skills.
4. Configure pull request triggers for \`opened\`, \`synchronize\`, \`reopened\`, and \`labeled\`. Do not run on drafts by default, but allow the \`Warden\` label to opt a draft PR in. Make sure the workflow event types and every skill's trigger agree.
5. Choose explicit, sensible defaults for this repository. Configure the current preferred model field under \`[defaults.agent]\`, plus appropriate \`failOn\`, \`reportOn\`, \`maxFindings\`, \`requestChanges\`, and \`failCheck\` behavior. Explain any choices that are not directly implied by the docs or repository. Do not use legacy config fields for a new setup.
6. Configure the model and runtime according to the Models and Runtimes docs. If the provider/model cannot be inferred from existing repository or organization conventions, ask me which provider and model to use before finalizing. Put the non-secret model selector in \`warden.toml\`; never put API keys in tracked files. Identify the exact local environment variable and GitHub Actions secret required for the selected provider, such as \`WARDEN_OPENAI_API_KEY\`, \`WARDEN_ANTHROPIC_API_KEY\`, or \`WARDEN_OPENROUTER_API_KEY\`.
7. Review the generated \`.github/workflows/warden.yml\` against the current workflow docs. Keep the recommended split \`mode: analyze\` then \`mode: report\` flow, the required checkout ref and permissions, and the matching provider secret. Use the standard GitHub token unless this repository already uses a Warden GitHub App or I request branded comments; if an app is used, create its installation token only after analysis as documented.
8. Account for GitHub secret restrictions. Do not expose secrets to fork pull requests. Note that Dependabot secrets are managed separately from Actions secrets, and tell me any safe manual configuration needed for the PR sources this repository accepts.
9. Ensure \`.warden/\` runtime output is ignored without removing unrelated ignore rules. Review every generated change and remove placeholders or unused provider secret references.
10. Validate the finished setup. Run the repository's formatting or documentation checks that cover changed files, then run Warden locally against the current branch or an explicit useful target with each configured built-in skill. If credentials are unavailable, still validate all static setup you can and report the exact environment variable needed to complete a live run. Fix setup and configuration errors, but do not change product code merely to satisfy review findings.
11. Summarize the final \`warden.toml\` behavior, files changed, commands run, validation results, required local environment variables, required GitHub secrets, and any remaining manual GitHub settings. The repository should be ready for both local reviews and pull request reviews when you finish.`;

## Set Up With Your Agent

Copy these instructions into your local coding agent to install and configure Warden in the current project.

<CopyPrompt content={setupPrompt} title="Set up Warden in this project" />

## Install

Install the CLI globally:

```bash
pnpm add -g @sentry/warden
```

## Authenticate

Warden runs model-backed work through Pi by default. Set the model selector and
the matching provider credential:

```bash
# OpenAI example
export WARDEN_MODEL=openai/gpt-5.5
export WARDEN_OPENAI_API_KEY=sk-...

# Anthropic example
export WARDEN_MODEL=anthropic/claude-sonnet-4-6
export WARDEN_ANTHROPIC_API_KEY=sk-ant-...
```

CI environments require the same provider credentials as repository or
organization secrets. See [Models and Runtimes](/config/models/) for config
defaults, per-skill overrides, and Claude runtime selection.

## Initialize

Initialize Warden in a repository:

```bash
warden init
```

`warden init` creates:

| File | Purpose |
| --- | --- |
| `warden.toml` | Skills, triggers, thresholds, and defaults. |
| `.github/workflows/warden.yml` | Pull request workflow for GitHub Actions. |

## Run Locally

```bash
warden
```

With no arguments, Warden reviews the current branch against the default branch.
Use this before opening or updating a pull request.

## Initial `warden.toml`

The default config starts with the built-in security and correctness reviews:

```toml title="warden.toml"
version = 1

[[skills]]
name = "security-review"

[[skills.triggers]]
type = "pull_request"
actions = ["opened", "synchronize", "reopened", "labeled"]
draft = false
labels = ["Warden"]

[[skills]]
name = "code-review"

[[skills.triggers]]
type = "pull_request"
actions = ["opened", "synchronize", "reopened", "labeled"]
draft = false
labels = ["Warden"]
```

Add more skills when your codebase has specific review needs.

## Next

<div class="api-link-grid">
  <a class="api-link-card" href="/local-review/">
    <strong>Local Review</strong>
    <span>Use staged, range, target, skill-specific, and autofix runs.</span>
  </a>
  <a class="api-link-card" href="/github/repository/">
    <strong>Repository Setup</strong>
    <span>Set up GitHub secrets and pull request reviews.</span>
  </a>
</div>
