---
import Base from '../layouts/Base.astro';
import Terminal from '../components/Terminal.astro';
import { Code } from 'astro:components';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<Base title="Home" description="Agents that review your code. Locally or on every PR.">
  <div class="hero">
    <p class="tagline">Your Code Is Under New Management</p>
    <p class="tagline-sub">Agents that review your code. Locally or on every PR.</p>

    <div class="cta-buttons">
      <a href={`${base}/setup`} class="btn btn-primary">Get Started</a>
    </div>
  </div>

  <section class="quick-start">
    <div class="step">
      <h3>Install</h3>
      <p>Install the CLI globally.</p>
      <Terminal title="Terminal" showCopy={true}>
        <Code
          code="npm install -g @sentry/warden"
          lang="bash"
          theme="vitesse-black"
        />
      </Terminal>
    </div>

    <div class="step">
      <h3>Initialize</h3>
      <p>Scaffold your project with config and GitHub workflow.</p>
      <Terminal title="Terminal" showCopy={false}>
        <pre class="cli-output"><span class="cli-dim">$</span> warden init

<span class="cli-green">Created</span> warden.toml
<span class="cli-green">Created</span> .github/workflows/warden.yml

<span class="cli-bold">Next steps:</span>
  1. Set <span class="cli-cyan">ANTHROPIC_API_KEY</span> in .env.local
  2. Add <span class="cli-cyan">ANTHROPIC_API_KEY</span> to repository secrets
     <span class="cli-dim">https://github.com/your-org/your-repo/settings/secrets/actions</span>
  3. Commit and open a PR to test</pre>
      </Terminal>
    </div>

    <div class="step">
      <h3>Add a Skill</h3>
      <p>Install a skill from the community. Vercel's React best practices is a great start.</p>
      <Terminal title="Terminal" showCopy={true}>
        <Code
          code="npx skills add https://github.com/vercel-labs/agent-skills --skill vercel-react-best-practices"
          lang="bash"
          theme="vitesse-black"
        />
      </Terminal>
    </div>

    <div class="step">
      <h3>Run Locally</h3>
      <p>Catch issues before you push. Fix them immediately.</p>
      <Terminal title="Terminal" showCopy={false}>
        <pre class="cli-output"><span class="cli-dim">$</span> warden HEAD --skill vercel-react-best-practices

Analyzing changes from <span class="cli-cyan">HEAD</span>...

<span class="cli-bold">FILES</span>  <span class="cli-cyan">3 files</span> <span class="cli-dim">· 5 chunks</span>
  <span class="cli-yellow">~</span> src/components/UserList.tsx <span class="cli-dim">(2 chunks)</span>
  <span class="cli-yellow">~</span> src/components/Dashboard.tsx <span class="cli-dim">(2 chunks)</span>
  <span class="cli-green">+</span> src/components/Modal.tsx <span class="cli-dim">(1 chunk)</span>

┌─ <span class="cli-bold">vercel-react-best-practices</span> ─────────────────────────── <span class="cli-dim">8.2s</span> ─┐
│ <span class="cli-bold">2 findings:</span> <span class="cli-yellow">●</span> 1 medium  <span class="cli-cyan">●</span> 1 low                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ <span class="cli-yellow">●</span> <span class="cli-bold">Missing React.memo for list items</span>                             │
│   <span class="cli-dim">src/components/UserList.tsx:15</span>                                │
│   <span class="cli-dim">15 │</span> const UserCard = (&#123; user &#125;) =&gt; &lt;div&gt;&#123;user.name&#125;&lt;/div&gt;; │
│                                                                 │
│   List item components should be memoized to prevent            │
│   unnecessary re-renders when parent state changes.             │
│                                                                 │
│ <span class="cli-cyan">●</span> <span class="cli-bold">useEffect missing dependency</span>                                   │
│   <span class="cli-dim">src/components/Dashboard.tsx:23</span>                               │
│   <span class="cli-dim">23 │</span> useEffect(() =&gt; &#123; fetchData() &#125;, [])                      │
│                                                                 │
│   The dependency array is missing 'fetchData'. This could       │
│   lead to stale closures or unexpected behavior.                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

<span class="cli-bold">SUMMARY</span>
<span class="cli-bold">2 findings:</span> <span class="cli-yellow">●</span> 1 medium  <span class="cli-cyan">●</span> 1 low
Analysis completed in <span class="cli-dim">8.2s</span></pre>
      </Terminal>
    </div>

    <div class="step">
      <h3>Keep an Eye on GitHub</h3>
      <p>Open a PR and Warden reviews it automatically. Findings appear as inline comments.</p>
      <div class="pr-mock">
        <div class="pr-mock-header">
          <div class="pr-mock-avatar">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z"/>
            </svg>
          </div>
          <div class="pr-mock-meta">
            <span class="pr-mock-name">warden</span>
            <span class="pr-mock-badge">bot</span>
            <span class="pr-mock-time">commented now</span>
          </div>
        </div>
        <div class="pr-mock-body">
          <div class="pr-mock-finding">
            <span class="pr-mock-severity severity-medium"></span>
            <span class="pr-mock-title">Missing React.memo for list items</span>
          </div>
          <div class="pr-mock-code">
            <div class="pr-mock-code-header">src/components/UserList.tsx</div>
            <pre class="pr-mock-code-content"><span class="line-num">15</span><span class="line-old">- const UserCard = (&#123; user &#125;) =&gt; &lt;div&gt;&#123;user.name&#125;&lt;/div&gt;;</span>
<span class="line-num">15</span><span class="line-new">+ const UserCard = React.memo((&#123; user &#125;) =&gt;</span>
<span class="line-num">16</span><span class="line-new">+   &lt;div&gt;&#123;user.name&#125;&lt;/div&gt;</span>
<span class="line-num">17</span><span class="line-new">+ );</span></pre>
          </div>
          <p class="pr-mock-desc">List item components should be memoized to prevent unnecessary re-renders when parent state changes.</p>
        </div>
        <div class="pr-mock-footer">
          <a href="#" class="pr-mock-link">View all 2 findings</a>
        </div>
      </div>
    </div>
  </section>

  <section class="section next-steps">
    <h2>Next Steps</h2>
    <div class="next-steps-grid">
      <a href={`${base}/skills`} class="next-step-card">
        <strong>Add your own skills</strong>
        <p>Create custom reviewers in <code>.warden/skills/</code></p>
      </a>
      <a href={`${base}/setup`} class="next-step-card">
        <strong>Configure triggers</strong>
        <p>Control when skills run with <code>warden.toml</code></p>
      </a>
    </div>
  </section>

</Base>

<style>
  .hero {
    text-align: center;
    padding: 3rem 0 2rem;
  }

  .tagline {
    font-family: var(--font-mono);
    font-size: 2.5rem;
    font-weight: 700;
    margin: 0;
    line-height: 1.1;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: #fff;
  }

  .tagline-sub {
    font-size: 1.25rem;
    color: var(--text-secondary);
    margin: 1rem 0 2rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }

  .btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    border-radius: 6px;
    font-weight: 600;
    text-decoration: none;
    transition: opacity 0.15s ease;
  }

  .btn-primary {
    background: #fff;
    color: #000;
  }

  .btn-primary:hover {
    opacity: 0.9;
    color: #000;
  }

  /* Quick Start Steps */
  .quick-start {
    margin-top: 3rem;
  }

  .step {
    margin-bottom: 2.5rem;
  }

  .step:last-child {
    margin-bottom: 0;
  }

  .step h3 {
    margin: 0 0 0.5rem;
    font-family: var(--font-mono);
    font-size: 1.125rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .step > p {
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }

  /* PR Mock */
  .pr-mock {
    background: #000;
    border: 1px solid #21262d;
    border-radius: 6px;
    overflow: hidden;
    font-size: 0.875rem;
    margin-top: 1rem;
  }

  .pr-mock-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    background: #0d1117;
    border-bottom: 1px solid #21262d;
  }

  .pr-mock-avatar {
    width: 32px;
    height: 32px;
    background: #21262d;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #8b949e;
  }

  .pr-mock-meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .pr-mock-name {
    font-weight: 600;
    color: #8b949e;
  }

  .pr-mock-badge {
    background: #238636;
    color: #fff;
    font-size: 0.6875rem;
    padding: 0.125rem 0.375rem;
    border-radius: 9999px;
    font-weight: 500;
    text-transform: uppercase;
  }

  .pr-mock-time {
    color: #8b949e;
    font-size: 0.75rem;
  }

  .pr-mock-body {
    padding: 1rem;
    background: #000;
  }

  .pr-mock-finding {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .pr-mock-severity {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .pr-mock-severity.severity-medium {
    background: #d29922;
  }

  .pr-mock-title {
    font-weight: 600;
    color: #9198a1;
  }

  .pr-mock-code {
    background: #0d1117;
    border: 1px solid #21262d;
    border-radius: 6px;
    overflow: hidden;
    margin-bottom: 0.75rem;
  }

  .pr-mock-code-header {
    background: #0d1117;
    padding: 0.5rem 0.75rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 600;
    color: #8b949e;
    border-bottom: 1px solid #21262d;
  }

  .pr-mock-code-content {
    margin: 0;
    padding: 0.5rem 0;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    line-height: 1.6;
    background: transparent;
    border: none;
    overflow-x: auto;
  }

  .pr-mock-code-content .line-num {
    display: inline-block;
    width: 2.5rem;
    padding: 0 0.5rem;
    color: #484f58;
    text-align: right;
    user-select: none;
  }

  .pr-mock-code-content .line-old {
    display: inline;
    background: rgba(248, 81, 73, 0.15);
    color: #f85149;
  }

  .pr-mock-code-content .line-new {
    display: inline;
    background: rgba(63, 185, 80, 0.15);
    color: #3fb950;
  }

  .pr-mock-desc {
    color: #8b949e;
    margin: 0;
    line-height: 1.5;
  }

  .pr-mock-footer {
    padding: 0.75rem 1rem;
    border-top: 1px solid #30363d;
  }

  .pr-mock-link {
    font-size: 0.8125rem;
    color: #58a6ff;
  }

  .pr-mock-link:hover {
    text-decoration: underline;
  }

  /* Next Steps */
  .next-steps-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }

  .next-step-card {
    display: block;
    padding: 1.25rem 1.5rem;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid var(--border);
    border-radius: 8px;
    transition: border-color 0.15s ease, background 0.15s ease;
    color: inherit;
  }

  .next-step-card:hover {
    border-color: var(--border-strong);
    background: rgba(255, 255, 255, 0.04);
    color: inherit;
  }

  .next-step-card strong {
    display: block;
    margin-bottom: 0.25rem;
    color: var(--text);
  }

  .next-step-card p {
    margin: 0;
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .next-step-card code {
    font-size: 0.8125rem;
  }

  @media (max-width: 768px) {
    .tagline {
      font-size: 1.25rem;
      letter-spacing: 0.02em;
    }

    .next-steps-grid {
      grid-template-columns: 1fr;
    }

    .step > p {
      margin-left: 0;
    }
  }
</style>
